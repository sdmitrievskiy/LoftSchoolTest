include /blocks/icon/icon

mixin socials(socials)
	// socials
	+b.socials&attributes(attributes)
		each social in socials
			+e('a').link(href=social.href, target="_blank")
				+icon(social.name)

	// end socials
