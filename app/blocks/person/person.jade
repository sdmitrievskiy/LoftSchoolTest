mixin person(imageSrc, name, email)
	// person
	+b.person&attributes(attributes)

		.image-wrapper
			+e('img').image(src=imageSrc)

		div
			+e('p').name= name
			+e('a').email(href= 'mailto:'+email)= email
	// end person
